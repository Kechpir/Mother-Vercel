/** Блоки записи на главный цикл (hero CTA, пакеты, таймер, форма). В Vercel: `0` — скрыть, иначе показать. */
export const SHOW_MAIN_COURSE_REGISTRATION =
  process.env.NEXT_PUBLIC_SHOW_MAIN_COURSE_REGISTRATION !== "0";
